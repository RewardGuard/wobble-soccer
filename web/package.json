{
  "name": "wobble-soccer-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Wobble Soccer — a fast 2.5D arcade soccer game (Three.js + TypeScript).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
