# --- core simulation + RL environment (always needed) ---
numpy>=1.23
gymnasium>=0.29

# Human play with graphics is the web app in web/ (Three.js + TypeScript) — see
# web/README.md.  The Python package here is the headless RL environment.

# --- training the example agent (optional: only for examples/train_ppo.py) ---
stable-baselines3>=2.3
torch>=2.0

# --- live reward monitoring (optional: rewardguard.dev integration) ---
rewardguard>=1.0
